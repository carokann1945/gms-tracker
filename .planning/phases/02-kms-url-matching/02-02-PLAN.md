---
phase: "02"
plan: "02-02"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "src/matcher.js"
autonomous: true
must_haves:
  - "`src/matcher.js` 파일이 새로 생성된다"
  - "`findKmsUrl(gmsEventName, kmsList)` 함수가 named export로 존재한다"
  - "함수는 GMS 이벤트 이름을 한국어로 번역하는 GPT-4o-mini 호출을 수행한다"
  - "번역된 이름과 `kmsList`를 GPT-4o-mini에 전달하여 최적 매칭 ID를 반환받는다"
  - "매칭 성공 시 `https://maplestory.nexon.com/News/Event/{id}` 형식의 URL을 반환한다"
  - "매칭 실패, GPT 에러, 입력값 없음 등 모든 실패 케이스에서 `null`을 반환하며 절대 throw하지 않는다"
---

# Plan 02-02: findKmsUrl() 구현 (src/matcher.js 신규 생성)

## Goal

`src/matcher.js`를 새로 생성하고 `findKmsUrl(gmsEventName, kmsList)` 함수를 작성한다. 이 함수는 GMS 영문 이벤트 이름을 GPT-4o-mini로 한국어 번역한 뒤, 사전에 로드된 KMS 이벤트 목록에서 GPT가 가장 유사한 항목을 찾아 URL을 반환한다. `kmsList`는 호출자(index.js)가 미리 로드하여 전달한다.

## Why This Plan Exists

- KMS 이벤트명은 한국어이고 GMS 이벤트명은 영어다. 직접 문자열 비교는 불가능하다.
- 한국어 게임 이벤트명 매칭은 편집 거리(edit distance) 같은 일반 알고리즘보다 GPT 판단이 훨씬 정확하다 — 줄임말, 브랜드명, "수정" 접두어 변형 등을 GPT가 처리한다.
- `findKmsUrl`이 내부에서 `fetchKmsEventList()`를 호출하면, 10개 이벤트 처리 시 KMS 전체 스크래핑이 10회 발생해 ~500초가 소요된다 (Pitfall 5). 해결책: `kmsList`를 파라미터로 받아 외부에서 1회만 로드하도록 한다.
- `src/ai.js`에 동일한 `gpt-4o-mini` + OpenAI 클라이언트 패턴이 이미 존재한다. 이 패턴을 그대로 재사용한다.

## Tasks

### T1: `src/matcher.js` 파일 생성

파일 전체를 아래와 같이 작성한다:

```javascript
import OpenAI from 'openai';

const client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

/**
 * GMS 이벤트 이름에 대응하는 KMS 이벤트 페이지 URL을 반환한다.
 * @param {string} gmsEventName - GMS 영문 이벤트 이름
 * @param {{ id: string, name: string }[]} kmsList - fetchKmsEventList()의 반환값
 * @returns {Promise<string|null>} KMS URL 또는 null
 */
export async function findKmsUrl(gmsEventName, kmsList) {
  if (!gmsEventName || !kmsList?.length) return null;

  try {
    // Step 1: GMS 이벤트 이름을 한국어로 번역
    const translateResp = await client.chat.completions.create({
      model: 'gpt-4o-mini',
      messages: [
        {
          role: 'system',
          content: '메이플스토리 GMS 이벤트 이름을 KMS에서 사용하는 한국어로 번역해 줘. 번역 결과만 출력해.',
        },
        { role: 'user', content: gmsEventName },
      ],
      max_tokens: 100,
      temperature: 0,
    });

    const translatedName = translateResp.choices[0]?.message?.content?.trim() ?? '';
    if (!translatedName) return null;

    // Step 2: KMS 이벤트 목록에서 GPT로 최적 매칭 ID 탐색
    const listText = kmsList.map((e) => `id=${e.id}: ${e.name}`).join('\n');

    const matchResp = await client.chat.completions.create({
      model: 'gpt-4o-mini',
      messages: [
        {
          role: 'system',
          content:
            '아래 KMS 이벤트 목록에서 주어진 이벤트 이름과 가장 유사한 항목을 찾아 해당 id만 숫자로 반환해 줘. ' +
            '유사한 항목이 없으면 "null"이라고만 대답해.',
        },
        { role: 'user', content: `찾는 이름: ${translatedName}\n\n목록:\n${listText}` },
      ],
      max_tokens: 20,
      temperature: 0,
    });

    const matchResult = matchResp.choices[0]?.message?.content?.trim() ?? '';
    if (!matchResult || matchResult.toLowerCase() === 'null') return null;

    // Pitfall 4 방지: GPT가 "id=1301" 또는 "The best match is 1301"처럼 반환할 수 있음
    const matchedId = matchResult.match(/\d+/)?.[0];
    if (!matchedId) return null;

    // Pitfall 3 방지: 최종 URL은 반드시 /News/Event/{id} 형식 (Closed 경로 아님)
    return `https://maplestory.nexon.com/News/Event/${matchedId}`;
  } catch (err) {
    console.error('[matcher] findKmsUrl error:', err.message);
    return null;
  }
}
```

### T2: 에러 처리 검증

- `gmsEventName`이 falsy → 즉시 `null` 반환 (GPT 호출 없음)
- `kmsList`가 비었거나 null → 즉시 `null` 반환
- GPT API 에러 → catch에서 `null` 반환, 파이프라인 중단 없음
- GPT가 `"null"` 문자열 반환 → `null` 반환
- GPT가 숫자 없는 텍스트 반환 → `matchResult.match(/\d+/)` 실패 → `null` 반환

## Constraints

- `fetchKmsEventList()` import 금지 — `kmsList`는 파라미터로 전달받음 (Pitfall 5 방지)
- `src/matcher.js`만 수정 — `src/fetcher.js`, `src/ai.js`, `index.js` 변경 없음
- `throw` 금지 — 모든 실패 케이스에서 `null` 반환
- 새 npm 패키지 설치 금지 — `openai` 패키지는 이미 설치됨 (v6.33.0)
- `process.env.OPENAI_API_KEY` 하드코딩 금지 — OpenAI 클라이언트 생성 시 `process.env.OPENAI_API_KEY` 주입

## Anti-Patterns

- **Pitfall 4 방지:** GPT 응답에서 `matchResult.match(/\d+/)?.[0]`로 숫자만 추출
- **Pitfall 3 방지:** 최종 URL 형식은 `/News/Event/{id}` (Closed 경로 포함하지 않음)
- **Pitfall 5 방지:** `fetchKmsEventList()` 함수를 내부에서 호출하지 않음

## Verification

- [ ] `src/matcher.js` 파일 생성 확인
- [ ] `findKmsUrl` named export 확인
- [ ] 함수 시그니처가 `findKmsUrl(gmsEventName, kmsList)` 형태 (2개 파라미터)
- [ ] GPT 번역 호출 → GPT 매칭 호출 순서 확인 (코드 리뷰)
- [ ] `matchResult.match(/\d+/)` 패턴으로 ID 추출 확인 (Pitfall 4 방지)
- [ ] 최종 URL이 `/News/Event/{id}` 형식 확인 (Pitfall 3 방지)
- [ ] `fetchKmsEventList` import 없음 확인 (Pitfall 5 방지)
- [ ] try-catch로 모든 에러를 `null`로 처리 확인
